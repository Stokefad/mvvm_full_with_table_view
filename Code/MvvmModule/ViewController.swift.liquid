//
//  {{ prefix }}{{ module_info.name }}{{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit

final class {{ module_info.name }}ViewController: ViewController {
    
    private let viewModel: I{{ module_info.name }}ViewModel
    private lazy var router: I{{ module_info.name }}Router = {{ module_info.name }}Router(controller: self)
    
    private let <#name#> = <#value#>
    
    init(viewModel: I{{ module_info.name }}ViewModel) {
        self.viewModel = viewModel
        super.init(nibName: "{{ module_info.name }}ViewController", bundle: nil)
    }
    
    required init?(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }
    
    override func viewDidLoad() {
        super.viewDidLoad()
    }
}

extension {{ module_info.name }}ViewController {
    private func <#name#>(<#parameters#>) -> <#return type#> {
        <#function body#>
    }
}
